---
dist: xenial
language: python
python: 3.7
services: docker
env:
- STEP=geni TAG=v0.9.9.2
install:
- git clone https://github.com/systemslab/popper /tmp/popper
- pip install /tmp/popper/cli
script:
- popper run -f ./.ci.yml
